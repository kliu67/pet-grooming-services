export const MAX_NAME_LENGTH = 60;
export const MAX_DESCRIPTION_LENGTH = 1000;

export const MIN_PASS_LENGTH = 8;
export const MAX_PASS_LENGTH = 72;

export const ROLES = {
    default: 'client',
    admin: 'admin',
    stylist: 'stylist',
    client: 'client'
}
